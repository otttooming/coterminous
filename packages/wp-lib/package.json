{
    "name": "@coterminous/wp-lib",
    "version": "0.1.0",
    "description": "Wordpress and WooCommerce REST API helpers",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": [
      "dist"
    ],
    "scripts": {
      "build": "npm run clean && tsc -p tsconfig.dist.json",
      "build:lib": "npm run build",
      "build:lib:watch": "npm run build:watch",
      "build:watch": "nodemon",
      "clean": "rm -rf dist"
    },
    "devDependencies": {
      "isomorphic-unfetch": "3.0.0",
      "nodemon": "1.18.9"
    }
  }
